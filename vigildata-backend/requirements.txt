fastapi==0.115.0
uvicorn[standard]==0.30.6
sqlalchemy==2.0.35
psycopg[binary]==3.2.3
pydantic[email]==2.9.2
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==3.2.2
python-dotenv==1.0.1
httpx==0.27.2
# Nota: las dependencias pesadas de ML (sentence-transformers / torch), de
# migraciones (alembic) y de tests viven en requirements-dev.txt. En produccion
# (Vercel) NO se instalan para no exceder el limite de tamano del bundle
# serverless; HU-09 usa el fallback de embedding por hashing
# (ver app/services/duplicados_bert.py).
